# testing_repo

Minimal Python example repository containing a single script that adds two numbers.

Files:
- `main.py` — defines add_two_numbers(a, b) and prints the sum when run.

Usage:

```bash
python3 main.py
```

Output:

```
The sum of 5 and 7 is 12
```